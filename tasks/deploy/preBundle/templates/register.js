/** generated code */
/**
 * () registers the function and the
 * callback to handle the aws event
 */
(() => {
  const func = /** func */;
  const conf = /** conf */;
  runtime.register(func, conf);
})();
